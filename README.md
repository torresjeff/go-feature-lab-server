# Feature Lab Server
Feature Lab Server is the backend for Feature Lab, a feature flag solution in Go.

Run the server:
```shell
$ docker-compose up -d
```

Stop the server:
```shell
$ docker-compose stop
```